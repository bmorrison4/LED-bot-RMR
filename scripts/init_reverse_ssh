echo This will create an entry in known hosts for root when it tries to connect so that ssh can be run from cron. Note: it doesn't matter that it won't actaully connect without the pem specified. It will still create the neccesary entry.
sudo -u root ssh rssh@34.216.85.34